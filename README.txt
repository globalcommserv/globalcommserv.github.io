GCS Website Update 36

Fixes the Joint Ventures navigation dropdown so it closes when the cursor leaves the menu on desktop.
Also closes on outside click, keyboard focus leaving the dropdown, or Escape.

Upload main.js to the GCS2 repository root and replace the existing file.
